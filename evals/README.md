# Evaluation Assets

`scenarios.json` contains fixed, evidence-bound product decisions for the blind
comparison described in `docs/EVAL_PLAN.md`. `scorecard.csv` is an empty results
template. Keeping scenarios and results separate prevents prepared showcase data
from being presented as empirical performance.

